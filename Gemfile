source "https://rubygems.org"

# Lock to the GitHub Pages gem set (includes Jekyll and approved plugins)
gem "github-pages", group: :jekyll_plugins

# Optional: If you later need to pin a Jekyll version explicitly, uncomment:
# gem "jekyll", "~> 3.9"

# To speed up future installs you can run: bundle config set path 'vendor/bundle'