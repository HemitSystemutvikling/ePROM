source "https://rubygems.org"

# Lock to the GitHub Pages gem which pins a compatible set of Jekyll + approved plugins.
# Version matrix: https://pages.github.com/versions/
gem "github-pages", group: :jekyll_plugins

# Optional extras you can uncomment if needed:
# gem "webrick"       # Only if Ruby 3.x complains WEBrick is missing
# gem "jekyll-watch"  # Live reload (already indirectly handled, usually not needed)
# gem "jekyll", "~> 3.9"  # Normally DO NOT pin; github-pages manages the version

# Development-only (example of adding explicit plugin versions). github-pages already includes jekyll-feed.
group :development do
  # gem "jekyll-feed", "~> 0.17"
end

# Keep dependencies out of Git. Ensure .gitignore contains:
# vendor/
# .bundle/
# _site/
